apiVersion: apps/v1
kind: Deployment
metadata:
  name: explainability-service
  namespace: oncoidt
  labels:
    app: explainability-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: explainability-service
  template:
    metadata:
      labels:
        app: explainability-service
    spec:
      containers:
        - name: explainability-service
          image: oncoidt/explainability-service:latest
          ports:
            - containerPort: 8006
          env:
            - name: SERVICE_NAME
              value: explainability-service
          envFrom:
            - configMapRef:
                name: oncoidt-config
            - secretRef:
                name: oncoidt-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: explainability-service
  namespace: oncoidt
spec:
  type: ClusterIP
  selector:
    app: explainability-service
  ports:
    - port: 8006
      targetPort: 8006
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: explainability-service
  namespace: oncoidt
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: explainability-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
