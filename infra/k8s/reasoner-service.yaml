apiVersion: apps/v1
kind: Deployment
metadata:
  name: reasoner-service
  namespace: oncoidt
  labels:
    app: reasoner-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reasoner-service
  template:
    metadata:
      labels:
        app: reasoner-service
    spec:
      containers:
        - name: reasoner-service
          image: oncoidt/reasoner-service:latest
          ports:
            - containerPort: 8004
          env:
            - name: SERVICE_NAME
              value: reasoner-service
          envFrom:
            - configMapRef:
                name: oncoidt-config
            - secretRef:
                name: oncoidt-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: reasoner-service
  namespace: oncoidt
spec:
  type: ClusterIP
  selector:
    app: reasoner-service
  ports:
    - port: 8004
      targetPort: 8004
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: reasoner-service
  namespace: oncoidt
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: reasoner-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
